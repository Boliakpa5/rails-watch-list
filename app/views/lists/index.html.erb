<h1>Lists</h1>

  <%= link_to "Create a new list", new_list_path %>

  <% @lists.each do |list| %>
    <li><%= link_to "#{list.name}", list_path(list)  %>
    <%= link_to "Delete", list_path(list), data: { turbo_method: :delete, turbo_confirm: "Delete #{list.name}?" } %>
    </li>
  <% end %>
</ul>
